name: Demo Build

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    - uses: ./.github/workflows/install-dotnet.yml
      with:
        version: 6.0.x